# Contributing

Thanks for helping maintain the Rhoe Homebrew tap.

Run the local validation gate before opening a pull request:

```bash
make check
```

Formula changes should be generated from the checked-in templates unless a
maintainer explicitly approves a manual emergency patch.
